# 库存管理系统简化总结 (v3.3 → v3.4)

## 执行时间
2026-06-26 22:00~22:30

## 核心变更

### 1. 彻底删除个体件跟踪
- **删除 `inventory_items` 表**：不再创建和查询该表
- **简化库存管理逻辑**：只按 `(part_number, location)` 管理总数量

### 2. 简化的功能

#### 出库（show_out_dialog）
- **之前**：查询个体件，逐个出库
- **现在**：只按数量扣减当前架位库存
- **逻辑**：`UPDATE inventory SET total_quantity=total_quantity-? WHERE part_number=? AND location=?`

#### 入库（show_in_dialog）
- **之前**：生成个体件记录（serial_number）
- **现在**：只增加总数量
- **逻辑**：`UPDATE inventory SET total_quantity=total_quantity+? WHERE part_number=? AND location=?`

#### Excel 导入（import_excel）
- **之前**：为每行生成个体件记录
- **现在**：只导入数量到 `inventory` 表

#### 主表格加载（load_inventory）
- **之前**：子查询 `inventory_items` 获取最早有效期
- **现在**：只查询 `inventory` 表（7列，无 `earliest_expiry`）

### 3. 删除的功能
- **`show_items_detail` 函数**：个体件详情查看
- **菜单项**："查看个体件详情"
- **按钮**：搜索栏旁的"个体件详情"按钮

### 4. 代码统计
- **删除行数**：230 行
- **新增行数**：80 行
- **净减少**：150 行
- **删除 `inventory_items` 引用**：7 行

## 用户需求对照

| 需求 | 状态 |
|------|------|
| 每行单独管理 | ✅ 按 `(part_number, location)` 独立管理 |
| 出库只扣除当前行库存 | ✅ `show_out_dialog` 只更新当前架位 |
| 不影响其他架位 | ✅ 按 `part_number+location` 精确更新 |
| 删除个体件详情功能 | ✅ 已删除 `show_items_detail` 及相关 UI |

## 测试建议

1. **导入 Excel**：确认只更新 `inventory` 表
2. **入库操作**：确认只增加总数量
3. **出库操作**：确认只扣除当前架位库存（不影响其他架位）
4. **主表格显示**：确认无报错，数据正确

## 遗留问题

- **无头环境无法完整测试**：GUI 应用需要显示环境
- **建议**：用户在有显示的环境下测试所有功能

## 提交信息

- **Commit**: `baec1ec`
- **分支**: `main`
- **超前**: 15 个提交（需要 push）

## 下一步

1. 用户测试所有功能
2. 如有报错，提供错误信息
3. 修复 bug
4. push 到 GitHub
5. 重新打包 exe
